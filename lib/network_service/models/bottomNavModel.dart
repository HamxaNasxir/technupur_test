import '../../../utils/enums.dart';

class BottomNavItem {
  final String navTitle;
  final String iconData;
  final MenuCode menuCode;

  const BottomNavItem({required this.navTitle, required this.iconData, required this.menuCode});
}
